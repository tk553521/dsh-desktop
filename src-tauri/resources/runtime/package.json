{
  "name": "dsh-desktop-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Bundled @deepseek-ai/dsh runtime manifest for DSH Desktop. The exact dependency graph is pinned in package-lock.json; scripts/prepare-runtime.mjs runs `npm ci` here to materialize src-tauri/resources/runtime/node_modules before the tauri build.",
  "dependencies": {
    "@deepseek-ai/dsh": "^0.1.0-rc.6"
  }
}
